# 学习进度

## 1. 当前阶段

- 当前阶段：第二阶段 AI Infra 转型路线
- 当前周次：第 15 周
- 当前主题：训练数据路径、checkpoint、通信链路与混部扩展点
- 当前目标：在已跑通 TrainJob + Scheduler Plugin + DDP 端到端链路的基础上，补齐训练任务的数据路径、checkpoint 恢复、通信与混部指标心智，但不追 CSI、NCCL、RDMA 的专家深度。
- 当前状态：第 13-14 周完整链路已验收；第 15 周已完成 rank0 PVC 写入、最小 `torch.save` checkpoint 文件验证，并明确 rank0-only checkpoint 的恢复边界。
- 下一步：进入通信链路扩展点，区分当前 kind + Gloo + Pod 网络实验与真实 GPU 多机训练中的 NCCL/RDMA 链路差异。

## 2. 第二阶段 AI Infra 转型 16 周 Checklist

| 周次 | 主题 | 状态 | 备注 |
| --- | --- | --- | --- |
| 第 1-2 周 | Kubernetes 调度器框架与 GPU 调度入口 | 已完成 | 已完成最小 scheduler 二进制、`Filter/Score` 插件、`schedulerName`/`PluginConfig` 注入与缺参失败策略闭环 |
| 第 3-4 周 | GPU 资源模型、NUMA、拓扑感知调度 | 已完成 | 已完成资源模型、device plugin/kubelet 链路、TopologyManager 边界与策略文档 v1 |
| 第 5-6 周 | 批任务调度、队列、抢占与 Gang Scheduling | 已完成 | 对应项目 A3：训练任务队列 CRD，涉及 TrainJob Controller, PodGroup, Permit 内存泄漏与幂等状态流转 |
| 第 7-8 周 | PyTorch DDP 最小训练实验 | 已完成 | 已完成 torchrun、rank、world size、process group、all-reduce、DDP gradient sync、step time、worker contract；DeepSpeed/Megatron 仅做边界认知，不作为本阶段验收项 |
| 第 9 周 | DDP 通信瓶颈与指标采集 | 已完成 | 已完成指标边界、数据慢/训练主体慢归因、all_reduce payload 对照、world_size 吞吐判断；NCCL/RDMA 先理解链路 |
| 第 10-12 周 | TrainJob Operator v2 接入 DDP worker | 已完成 | 已完成 CRD 对象模型、rank0 ClusterIP Service、worker Pod env 注入、attempt 隔离、状态聚合、失败重试与真实 DDP worker kind 验证 |
| 第 13-14 周 | Scheduler Plugin + TrainJob + DDP 完整链路演示 | 已完成 | 已完成 TrainJob worker `schedulerName`/extended resource 注入、Node status resource patch、v1.29 `ScorePlugin` 适配、`NodeResourcesFit + NodeLabelScore` 调度闭环与 DDP worker 成功运行 |
| 第 15 周 | CSI/存储、NCCL/RDMA、混部扩展点 | 进行中 | 只补训练数据路径、checkpoint、通信链路和混部指标心智，不追专家深度；先从 checkpoint 路径与 Pod 生命周期边界切入 |
| 第 16 周 | 项目收口与面试表达 | 未开始 | 收敛 3 个核心项目与完整面试叙事 |

## 3. 实验记录 (第二阶段)

### 2026-03-31
(此处保留原第1-2周实验记录摘要...)

### 2026-04-17
- 实验主题：第 5-6 周批任务调度对象模型第一轮拆解
- 关键入口：Volcano PodGroup、minMember、Queue、priorityClassName、preemption、TrainJob CRD spec 设计
- 当前结论：
    - 普通 Job 的 completions/parallelism 是统计语义，没有启动时序的原子性约束，无法表达 gang scheduling
    - Volcano 通过 PodGroup.minMember 把调度决策粒度从单 Pod 提升到整组
    - Queue 解决资源边界，priorityClassName 解决同队列内竞争顺序，preemption 解决高优先级任务抢占低优先级占用资源的场景
    - TrainJob 是用户侧声明对象，PodGroup 是调度侧协议对象，controller 负责把前者翻译成后者
    - Volcano 不替换 kube-scheduler，而是并存，通过 schedulerName 字段认领自己负责的对象

### 2026-04-24
- 实验主题：第 5-6 周 TrainJob Controller 状态机流转与幂等性实现
- 关键入口：`AIInfraTrainJob` 仓库, Reconciler loop, Informer caching, IndexField, OwnerReference
- 当前结论：
    - TrainJob Controller 完整实现了基于 Reconciler loop 的状态机流转，保证了状态更新的幂等性。
    - 深入理解了 Informer 机制与 Local Cache (`IndexField`)，通过 `OwnerReference` 建立了 TrainJob 与底层 Pod 的级联关系。
    - 验证了纯 Plugin 在 Permit 阶段可能产生的内存泄漏问题，并掌握了使用 Controller 管理状态的优势。
    - 双通道监听：`Watches` 用于监听全局 Pod 释放资源以唤醒 `Queued` 任务，`Owns` 用于精准监听自己名下 Pod 的状态以实现 `Running -> Finished` 的聚合。
- 下一步：进入第 7-8 周，从 Docker-level networking 和 raw TCP (`nc`) 验证开始，探索分布式训练底层进程如何互相发现并握手。

### 2026-04-28
- 实验主题：第二阶段后半程优先级调整
- 关键入口：`转型评估与规划基准.md`、`AIInfra转型路线.md`
- 当前结论：
    - 后续不再平均推进 RDMA、CSI、DeepSpeed、Megatron、混部等主题。
    - 主线收敛为三个项目：TrainJob Operator、GPU-aware Scheduler Plugin、DDP 训练实验平台。
    - 第 7-8 周继续推进 DDP，但只保留 `torchrun`、`rank`、`world size`、`process group`、`all-reduce`、`gradient sync` 和 step time 指标。
    - DeepSpeed、Megatron、RDMA、CSI 暂时作为边界认知和面试扩展点，不作为当前主线。
- 下一步：用 Docker + `nc` 验证两个分布式进程之间的最小 TCP 握手模型。

### 2026-04-30
- 实验主题：第 7-8 周 DDP 最小训练实验主线推进
- 关键入口：`../AIInfraDDPLab`
- 当前结论：
    - 已从 raw TCP / `nc` 握手映射到 DDP 的 `MASTER_ADDR`、`MASTER_PORT`、`RANK`、`WORLD_SIZE`。
    - 已验证 `torchrun` 会在 `init_process_group()` 之前为 worker 注入环境变量。
    - 已验证 `gloo` + CPU tensor 的 `all_reduce SUM`，并观察到每个 rank 都会拿到相同聚合结果。
    - 已验证单 GPU 上 `backend="nccl"` + 2 rank + CUDA tensor 会因重复 GPU 映射失败，结论是当前机器适合用 Gloo 学多进程语义，用单进程 CUDA 验证 GPU 能力。
    - 已通过手工 `all_reduce` 和 DDP 对照实验验证：DDP 把梯度同步接入了 `loss.backward()`，没有 DDP 时多个 rank 只是各自训练本地模型副本。
    - 已引入 `step_time_ms`、`max_step_time_ms`、`data_time_ms`、`compute_sync_time_ms`、`samples_per_second`，建立了最小性能观测格式。
    - 已观察到慢 rank 会拖住整个同步训练 group，且仅靠总 step time 无法区分数据、计算、通信瓶颈。
    - 已明确 DDP worker 最小启动契约：`MASTER_ADDR`、`MASTER_PORT`、`RANK`、`WORLD_SIZE`、`LOCAL_RANK`。
- 阶段验收：
    - 能解释 `torchrun` 是启动器，负责拉起多个 worker 并注入 rank/world_size/master 等环境变量。
    - 能解释 `init_process_group()` 负责让当前进程加入同一个通信组。
    - 能解释 DDP 不是只启动多个进程，而是在 `backward()` 阶段自动 all-reduce 梯度。
    - 能解释慢 rank 会拖住同步 DDP 任务，因为 collective communication 需要所有 rank 参与。
    - 能解释 TrainJob Operator 后续至少要注入 `MASTER_ADDR`、`MASTER_PORT`、`RANK`、`LOCAL_RANK`、`WORLD_SIZE`。
    - DeepSpeed / Megatron 仅保留边界认知：它们是 DDP 之后的大模型训练扩展方向，当前不深入。
- 当前状态：第 7-8 周已完成。
- 下一步：进入第 9 周 DDP 通信瓶颈与指标采集，基于现有 DDP 实验工具做指标归因。

### 2026-04-30
- 实验主题：第 9 周 DDP 通信瓶颈与指标采集
- 关键入口：`../AIInfraDDPLab/docs/stage-06-metric-diagnosis.md`、`../AIInfraDDPLab/experiments/06_all_reduce_payload.py`
- 当前结论：
    - `max_step_time_ms` 是 DDP group 的整体 step time 近似指标，不能直接定位根因。
    - `samples_per_second` 是吞吐结果指标，不能单独判断计算、通信、数据或资源争用瓶颈。
    - `data_time_ms` 异常高时，优先怀疑数据加载、预处理、缓存、远端存储或 DataLoader worker。
    - `compute_sync_time_ms` 异常高时，优先怀疑训练主体路径，但当前粗指标不能直接区分 forward、backward、optimizer 和通信同步。
    - 慢 rank 会拖住同步 DDP 任务，其他 rank 可能在同步点等待。
    - 手工 `all_reduce` payload 增大后，`all_reduce_time_ms` 整体上升，说明通信量本身可能成为 step time 的重要组成部分。
    - 当前 CPU / Gloo / 单机共享资源环境下，`world_size=2` 不一定比 `world_size=1` 吞吐更高，因为没有新增独立计算资源，还增加了进程调度、通信和资源争用开销。
- 阶段验收：
    - 能区分结果指标和归因指标。
    - 能用粗指标完成数据路径和训练主体路径的一级归因。
    - 能解释粗指标不能过度解释，区分计算慢和通信慢需要更细指标或纯通信对照实验。
    - 能解释 `all_reduce` payload 变大导致通信耗时上升的原因。
- 当前状态：第 9 周已完成。
- 下一步：进入第 10-12 周，先将第 7-9 周 DDP 知识整理成面向 TrainJob Operator 的平台语义图。

### 2026-05-09
- 实验主题：第 10-12 周 TrainJob Operator v2 接入 DDP worker 控制面改造
- 关键入口：`../AIInfraTrainJob/api/v1/trainjob_types.go`、`../AIInfraTrainJob/internal/controller/trainjob_controller.go`、`../AIInfraDDPLab/docs/stage-07-platform-semantics.md`
- 当前结论：
    - `worldSize` 属于 `spec`，语义是 DDP 通信组大小，不只是旧版 `minMember` 的 Pod 数量替代；controller 会把它注入为 worker 的 `WORLD_SIZE`。
    - `masterPort` 属于 `spec`，默认可按 `29500` 处理；`MASTER_ADDR` 不应由用户填写，而应由 controller 根据 rank0 Service 派生。
    - `rank` 不属于用户 `spec`，由 controller 按 `worldSize` 派生，并写入 worker Pod label 与 `RANK` env。
    - `attempt` 属于 `status`，由 controller 管理，用于区分同一 TrainJob 的不同重试轮次；它必须参与 worker Pod label 和 rank0 Service selector，避免旧 attempt 的 rank0 被 Service 错选。
    - 第一版采用 controller 直接创建 worker Pods + 普通 ClusterIP rank0 Service；当前不需要 StatefulSet + Headless Service，因为 DDP 第一版只需要稳定 rendezvous 入口，不需要稳定 Pod DNS 身份。
    - rank0 Service selector 最小集合为 `trainjob-name=<job>`、`attempt=<attempt>`、`rank=0`；worker Pod label 最小集合为 `trainjob-name=<job>`、`attempt=<attempt>`、`rank=<rank>`。
    - worker Pod 注入最小 DDP env：`MASTER_ADDR`、`MASTER_PORT`、`WORLD_SIZE`、`RANK`、`LOCAL_RANK`；当前模型是 1 Pod = 1 worker process，因此 `LOCAL_RANK=0`。
    - `RestartPolicy` 采用 `Never`，因为第一版失败恢复是 attempt 级别，不允许 kubelet 在同一个 Pod 内自动重启失败 worker 并假装恢复原 DDP group。
    - `TrainJob.status` 中保留 `runningWorkers`、`readyWorkers`、`lastFailure`。其中 `Running` 来自 `PodStatus.Phase`，`Ready` 来自 Pod condition，二者不是同一维度。
    - `TrainJobPhase` 中不单独设置 `Ready` phase；`Running` 是生命周期阶段，`Ready` 更适合作为 condition 或聚合计数表达。
    - kind 验证已通过 happy path：busybox worker 能按 `worldSize=2` 展开，rank0 Service 创建成功，worker env 注入正确，Pod 正常 `Completed` 后 TrainJob phase 进入 `Succeeded`。
    - kind 验证已通过失败重试路径：`rank=1` 执行 `exit 1` 后 Pod 进入 `Error`，controller 记录 `lastFailure.rank=1`、`exitCode=1`，`attempt` 从 0 推进到 1，Service selector 更新到新 attempt，旧 attempt worker Pods 被清理。
    - 当前 demo 策略保留最后一轮失败现场：当 `retryLimit` 耗尽后，TrainJob phase 进入 `Failed`，最后一轮 attempt 的 Pod 保留用于 `kubectl logs` / `describe` 排障；这和“失败即清理所有同轮 worker”的严格 DDP group 语义存在取舍。
- 阶段验收：
    - 能解释 `ClusterIP Service` 在当前 DDP Operator v2 中只承担 rendezvous 稳定入口，后续 rank 间连接依赖 Pod-to-Pod 网络和 backend 自己建立连接。
    - 能区分 `ClusterIP Service` 与 `Headless Service` 的核心差异：前者隐藏成员并提供统一入口，后者暴露具体成员并适合稳定成员身份场景。
    - 能解释 `List/Watch/Informer/Reconcile` 的链路：对象变化经 API Server watch 进入 informer/cache/workqueue，最终触发 `Reconcile`，不是 etcd 直接调用 controller。
    - 能解释 `OwnerReference` 只负责 owner 删除时的级联清理，不负责 attempt 之间的业务隔离；attempt label 才是 Service selector 隔离旧/新 worker 的关键。
    - 能解释 `exit 1` 导致 Pod `Error` 的原因：容器主进程非 0 退出，`RestartPolicy=Never` 下 Pod phase 变为 `Failed`。
- 当前状态：TrainJob Operator v2 控制面 demo 已完成第一轮 kind 验证。
- 下一步：将 busybox 观察脚手架替换为真实 DDP worker 启动命令，验证 Operator 注入的 `MASTER_ADDR`、`MASTER_PORT`、`RANK`、`WORLD_SIZE` 能驱动 worker 完成 `init_process_group()`。

### 2026-05-11
- 实验主题：第 10-12 周 TrainJob Operator v2 接入真实 DDP worker 验收
- 关键入口：`../AIInfraTrainJob`、`../AIInfraDDPLab/experiments/05_ddp_step_time.py`、kind 集群
- 当前结论：
    - 已构建 `aiinfra-ddp-worker:dev` 镜像，并加载到 kind 集群节点中；worker Pod 不再使用 busybox 脚手架，而是直接运行 `python experiments/05_ddp_step_time.py`。
    - Operator 注入的 `MASTER_ADDR`、`MASTER_PORT`、`RANK`、`WORLD_SIZE`、`LOCAL_RANK` 能驱动 `dist.init_process_group(backend="gloo")` 成功完成初始化。
    - `torchrun` 在当前 Operator 模式下不再作为启动器使用；rank/world_size/master 信息由 TrainJob Controller 分配并注入，每个 Pod 直接运行一个 DDP worker 进程。
    - happy path 验证通过：`worldSize=2` 时两个 rank 均输出 JSON 指标，日志中 `rank` 分别为 0/1，`world_size` 均为 2，TrainJob 最终进入 `Succeeded`。
    - 负向验证通过：新增 `--fail-before-init-rank` 脚手架参数后，指定 rank 可在 `init_process_group()` 前退出；controller 能识别 failed Pod，并将 TrainJob 标记为 `Failed`，`lastFailure` 记录失败 rank 与 exit code。
    - 慢 rank 验证通过：在 Kubernetes 中由 Operator 拉起的 DDP worker 能复现慢 rank 导致 group step time 上升；当前粗指标能证明 group 被慢 rank 拖慢，但不能区分主动 sleep 与被动等待。
    - 当前指标边界明确：`TrainJob.status` 保留低频控制面状态和失败摘要；每 step 的训练性能指标继续留在 worker 日志/后续 metrics pipeline，不塞入 CRD status。
- 阶段验收：
    - 能解释 Operator 如何替代 `torchrun` 的启动器职责：为每个 worker Pod 分配并注入 `RANK/WORLD_SIZE/MASTER_ADDR/MASTER_PORT/LOCAL_RANK`。
    - 能解释 `env://` 初始化方式下，`dist.init_process_group()` 依赖环境变量完成 rendezvous，而不是自行分配 rank。
    - 能解释 `WORLD_SIZE` 是强契约：缺失任一 rank 时，其他 rank 会等待、超时或失败，而不是降级运行。
    - 能解释真实 DDP worker 的成功、失败和慢 rank 现象如何映射回 TrainJob status、Pod phase 与 worker 日志。
- 当前状态：第 10-12 周已完成。
- 下一步：进入第 13-14 周，将 TrainJob worker Pod 的 `schedulerName` 与资源请求接入自定义 Scheduler Plugin，形成 Scheduler Plugin + TrainJob + DDP 的完整链路演示。

### 2026-05-12
- 实验主题：第 13-14 周 Scheduler Plugin + TrainJob + DDP 链路启动与阻塞定位
- 关键入口：`../AIInfraTrainJob`、`../AIInfraSchedulerPlugin`、kind v1.29.2 集群
- 当前结论：
    - TrainJob Controller 已经把调度意图写入 worker Pod：`spec.schedulerName=my-custom-scheduler`，并透传 `resources.requests.aiinfra.leon.com/gpu-capacity=1024000`。
    - `kubectl describe pod trainjob-sample-attempt-0-rank-0` 中 `Node=<none>` 且 `Events=<none>`，说明默认 scheduler 不会处理该 Pod，负责该 `schedulerName` 的自定义 scheduler 尚未完成 bind。
    - 旧版 `AIInfraSchedulerPlugin` 依赖混杂：kind API Server 为 `v1.29.2`，但 scheduler 二进制依赖中存在 `k8s.io/* v0.34.4`、`k8s.io/kubernetes v1.34+ / v1.35+`，启动后会 watch 当前集群不存在的 `ResourceSlice`、`DeviceClass`、`ResourceClaim`。
    - 依赖阻塞已处理：`go.mod` 中 Kubernetes staging modules 已统一 replace 到 `v0.29.2`，`k8s.io/kubernetes` 对齐到 `v1.29.2`，并清理了会拉高依赖的 `kube-openapi`、`structured-merge-diff/v6`、`cel-go`、`gnostic-models` 等残留版本冲突。
    - 当前 `AIInfraSchedulerPlugin` 已能通过 `GOCACHE=/tmp/go-build-cache go test ./...`。
    - 复验确认：`go test ./...` 仍然通过，但启动 scheduler 时失败于 profile 初始化阶段，错误为 `plugin "NodeLabelScore" does not extend ScorePlugin plugin`；这说明接口错位不是普通编译期错误，而是在 scheduler framework 将插件挂入 `Score` 扩展点时暴露。
    - 启动 scheduler 后暴露的是下一层问题：v1.29 scheduler framework 的插件接口签名与当前插件代码不完全一致；`Filter` 在 v1.29 接收 `*framework.CycleState` 与 `*framework.NodeInfo`，`Score` 在 v1.29 接收 `nodeName string`，需要通过 scheduler snapshot 读取 `NodeInfo`。
    - 已重新区分 informer cache、scheduler cache 与 scheduling cycle snapshot：informer cache 保存 API Server List/Watch 得到的原始对象；scheduler cache 维护调度语义结构如 `NodeInfo`、assumed Pod、nodeTree；snapshot 是单次调度周期中供 Filter/Score 等插件读取的稳定视图。
- 当前状态：第 13-14 周进行中；环境依赖阻塞已解除，framework 接口适配留给用户继续完成。
- 下一步：用户适配 `NodeLabelScore` 到 v1.29 `Score(ctx, state, pod, nodeName)` 签名，在插件结构中保存 `framework.Handle`，并通过 `handle.SnapshotSharedLister().NodeInfos().Get(nodeName)` 获取当前调度快照中的 Node 信息。

### 2026-05-12
- 实验主题：第 13-14 周 Scheduler Plugin + TrainJob + DDP 完整链路验收
- 关键入口：`../AIInfraSchedulerPlugin/pkg/plugin/node_label_score.go`、`../AIInfraSchedulerPlugin/cmd/scheduler-plugin/scheduler-config.yaml`、`../AIInfraTrainJob/config/samples/batch_v1_trainjob.yaml`
- 当前结论：
    - `NodeLabelScore` 已适配 Kubernetes v1.29 `Score(ctx, state, pod, nodeName)` 签名，并在插件工厂函数中保存 `framework.Handle`，通过 scheduler snapshot 读取 `NodeInfo`。
    - 当前路线已从 label-only 模拟切换到 extended resource 路线：`Pod.resources.requests.aiinfra.leon.com/gpu-capacity` 交给原生 `NodeResourcesFit` 判断，`Node.status.capacity/allocatable` 通过 patch 模拟节点资源账本。
    - `NodeLabelFilter` 原有逻辑本质上是简化版 `NodeResourcesFit`，在 extended resource 路线中会造成 label 与 allocatable 双账本，因此已从 scheduler profile 中移除；当前保留 `NodeLabelScore` 负责排序策略。
    - 重新清理 TrainJob、worker Pod、rank0 Service 和 Node extended resource 后，从头创建 TrainJob，验证 `my-custom-scheduler` 能接管 `schedulerName=my-custom-scheduler` 的 worker Pod。
    - 调度链路已闭环：TrainJob Controller 创建 worker Pod；scheduler 通过 `NodeResourcesFit` 校验 extended resource；`NodeLabelScore` 读取 `node_topological_hint` 打分；Pod 被 bind 到 Node。
    - DDP 链路已闭环：worker Pod 通过 controller 注入的 `MASTER_ADDR`、`MASTER_PORT`、`RANK`、`WORLD_SIZE` 完成 `init_process_group()`，日志输出 rank/world size/step 指标，TrainJob 最终进入成功状态。
    - 已明确 `informer` 不是独立进程，而是 scheduler 进程内的 informer/reflector goroutine；informer cache 保存 API Server 原始对象，scheduler cache 维护 `NodeInfo`、assumed Pod、nodeTree 等调度视图。
- 阶段验收：
    - 能解释为什么 `requests` 必须匹配 `Node.status.allocatable`，而不是匹配 Node label。
    - 能解释 label-only 模拟与 extended resource 模拟的差异：前者适合观察插件链路，后者更接近 Kubernetes 真实资源模型。
    - 能解释 `NodeResourcesFit`、`NodeLabelScore`、`schedulerName`、TrainJob Controller、DDP rendezvous 之间的职责边界。
    - 能从 `TrainJob -> worker Pod -> scheduler bind -> kubelet run -> DDP init -> Pod status -> TrainJob status` 串起完整端到端链路。
- 当前状态：第 13-14 周已完成。
- 下一步：进入第 15 周，先观察当前 worker Pod 的 volume / volumeMount，判断 checkpoint 如果写在容器本地文件系统，attempt 重试后为什么无法恢复。

### 2026-05-12
- 实验主题：第 15 周 checkpoint PVC 链路与 rank0 写入验证
- 关键入口：`../AIInfraTrainJob/api/v1/trainjob_types.go`、`../AIInfraTrainJob/internal/controller/trainjob_controller.go`、`../AIInfraDDPLab/experiments/05_ddp_step_time.py`
- 当前结论：
    - 当前 worker Pod 默认只有 Kubernetes 自动注入的 `kube-api-access-*` projected volume；它不是训练数据或 checkpoint 存储。
    - 容器内没有 `volumeMount` 覆盖的普通路径写入的是容器 writable layer，通常落在节点磁盘上，但生命周期跟容器/Pod 绑定，不能作为 checkpoint 持久化方案。
    - `volumes` 表示 Pod 要使用的外部存储来源，`volumeMounts` 表示把该来源挂到容器内哪个路径；二者通过 `name` 对应。
    - PVC 是 `PersistentVolumeClaim`，语义是存储申请单；Pod 引用 PVC，PVC 绑定 PV，PV 背后才是真实存储实现。
    - kind 默认 `standard` StorageClass 使用 `rancher.io/local-path`，`volumeBindingMode=WaitForFirstConsumer`，因此 PVC 初始 Pending，只有出现第一个使用它的 Pod 后才绑定 PV。
    - 当前第一版 checkpoint 策略收敛为：只让 rank0 挂载 checkpoint PVC 并写 marker，避免多 rank 并发写和 RWO 跨节点挂载问题。
    - TrainJob CRD 已能表达 `checkpointSpec.enabled`、`checkpointSpec.mountPath`、`checkpointSpec.pvcName`；controller 已按 `rank == 0` 给 worker Pod 注入 checkpoint PVC volume 和 volumeMount。
    - DDP worker 已支持 `--checkpoint-dir`，rank0 会在训练结束后写入 `marker.txt`。
    - 已通过 PV 绑定节点和 hostPath 路径验证：`marker.txt` 确实落到 local-path PV 背后的 kind node 存储中，内容为 `checkpoint update`，说明 rank0 写入 PVC 链路已打通。
- 当前状态：checkpoint PVC 最小链路已完成。
- 下一步：从 marker 文件升级到最小 `torch.save`，只保存 rank0 的模型参数和 step 元信息，先不做自动恢复。

### 2026-05-13
- 实验主题：第 15 周最小 PyTorch checkpoint 文件写入验证
- 关键入口：`../AIInfraDDPLab/experiments/05_ddp_step_time.py`、`../AIInfraTrainJob/config/samples/batch_v1_trainjob.yaml`
- 当前结论：
    - DDP worker 已支持 `--checkpoint-dir=/checkpoint`，该路径与 TrainJob rank0 worker 的 PVC mountPath 对齐。
    - rank0 在训练结束后通过 `torch.save` 写入 `/checkpoint/latest.pt`，保存内容包含 `model_state_dict`、`steps`、`world_size`。
    - 重新构建 `aiinfra-ddp-worker:dev` 镜像并加载到 kind 后，重新创建 TrainJob，已在 local-path PV 背后的 kind node 路径中观察到 `marker.txt` 与 `latest.pt`。
    - 这说明当前链路已经从“能写普通 marker 文件”推进到“能把最小 PyTorch checkpoint 文件写入 PVC”。
- 当前状态：rank0 最小 checkpoint 文件写入已完成。
- 下一步：分析当前 checkpoint 内容的恢复边界：只保存模型参数和 step 元信息还缺 optimizer state、随机数状态、数据迭代位置等训练恢复要素。

### 2026-05-13
- 实验主题：第 15 周 rank0-only checkpoint 恢复边界
- 关键入口：`../AIInfraDDPLab/experiments/05_ddp_step_time.py`、`../AIInfraTrainJob/internal/controller/trainjob_controller.go`
- 当前结论：
    - checkpoint 内容已从普通 marker 文件推进到最小 PyTorch checkpoint，保存 `model_state_dict`、`optimizer_state_dict`、`last_step`、`world_size` 这类核心字段。
    - `model_state_dict` 的恢复应发生在 `DDP(model)` 之前，因为当前保存的是 `ddp_model.module.state_dict()`，对应原始模型参数名。
    - `optimizer_state_dict` 的恢复应发生在 optimizer 创建之后，因为 optimizer state 绑定参数对象。
    - 当前第一版只给 rank0 挂 checkpoint PVC，因此 rank1/rank2 不能直接从 `/checkpoint/latest.pt` 读取同一份文件。
    - rank0-only checkpoint 方案只验证“rank0 能持久化训练状态”；如果要做完整多 rank 自动恢复，要么让所有 rank 都能访问共享 checkpoint 存储，要么由 rank0 读取后通过分布式通信广播参数和必要状态。
    - 当前阶段不继续实现自动恢复，避免被分布式 optimizer state、随机数状态、数据迭代位置和多 rank 状态同步拖偏。
- 当前状态：checkpoint 持久化边界已收口。
- 下一步：进入通信链路扩展点，先区分当前 `backend=gloo` 经过 Pod 网络通信，与真实 GPU 多机训练中 NCCL/RDMA 通信路径的差异。

## 4. 阶段复盘 (第二阶段)
(保留第 3-4 周复盘内容...)

## 5. 衔接规则
1. `当前阶段`
2. `下一步`
3. `最近一次实验记录`

# 学习进度

## 1. 当前阶段

- 当前周次：第 11 周
- 当前主题：Kubelet、CRI、containerd、runc 调用链
- 当前目标：先建立 `kubectl apply` 之后，请求如何从控制面一路落到节点与容器运行时的最小心智模型
- 当前状态：进行中
- 下一步：从 `kubectl apply` 提交一个最小 Pod 开始，只追第一个真正接住请求的控制面组件和对象持久化位置

## 2. 12 周阶段 Checklist

| 周次 | 主题 | 状态 | 备注 |
| --- | --- | --- | --- |
| 第 1 周 | Docker 基础命令与容器生命周期 | 已完成 | 已完成 image / container / process / writable layer 的第一轮认知验收 |
| 第 2 周 | Dockerfile、镜像分层与 OverlayFS 初识 | 已完成 | 已完成最小 Dockerfile、缓存链、GraphDriver 与 OverlayFS 存储痕迹验收 |
| 第 3 周 | 容器网络与端口映射 | 已完成 | 已完成 bridge、容器名解析、docker-proxy 与宿主机访问路径验收 |
| 第 4 周 | Volume、Bind Mount 与挂载视角 | 已完成 | 已完成 bind mount、volume 与挂载路径生效范围验收 |
| 第 5 周 | Namespace 深入与最小容器雏形 | 已完成 | 已完成 UTS、PID、Mount 三条骨架与最小容器初始化链收口 |
| 第 6 周 | cgroup、capabilities 与容器安全边界 | 已完成 | 已完成 cgroup v2、memory.max 与 capabilities 边界验收 |
| 第 7 周 | Kubernetes 核心对象入门 | 已完成 | 已完成 Pod、Deployment、Service 及其关系的第一轮验收 |
| 第 8 周 | ConfigMap、Secret、Volume 与声明式配置 | 已完成 | 已完成 ConfigMap、Secret 与注入方式差异验收 |
| 第 9 周 | 调度、资源请求限制与探针 | 已完成 | 已完成 Pending / OOMKilled / readiness / liveness 四类现象边界验收 |
| 第 10 周 | Kubernetes 网络模型 | 已完成 | 已完成 CoreDNS / kube-proxy / CNI(kindnet) / Service / Endpoints 职责边界与单节点网络链路验收 |
| 第 11 周 | Kubelet、CRI、containerd、runc 调用链 | 进行中 | 从 `kubectl apply` 到 apiserver / etcd 的第一跳开始 |
| 第 12 周 | 控制面与综合复盘 | 未开始 |  |

状态约定：

- 未开始
- 进行中
- 已完成
- 阻塞

## 3. 实验记录

### 2026-03-13

- 实验主题：初始化学习路线与进度基线
- 现象：仓库已建立 `docker/` 目录，学习入口已经具备，但尚未开始具体 Docker 实验
- 关键入口：`docker/` 目录、`学习路线.md`、`学习进度.md`
- 遇到的问题：暂无
- 当前结论：后续所有 Docker 相关源码实验、笔记和阶段性产出都应收敛在 `docker/` 目录内；路线和进度文档放在根目录做全局索引
- 下一次继续点：开始第 1 周实验，记录基础 Docker 命令对应的对象变化与疑问

### 2026-03-13

- 实验主题：检查 Docker 环境可用性
- 现象：`docker --version` 可执行，但 `docker info` 访问 daemon 失败，报错指向 `/var/run/docker.sock` 权限不足
- 关键入口：`docker --version`、`docker info`、`id`、`ls -l /var/run/docker.sock`
- 遇到的问题：当前用户 `litao` 不在 `docker` 组；`/var/run/docker.sock` 属于 `root:docker`，权限为 `srw-rw----`
- 当前结论：Docker CLI 已安装，但当前会话还不具备和 Docker daemon 通信的权限，因此还不能进入真正的容器生命周期实验
- 下一次继续点：先处理 Docker socket 权限，再回到第 1 周基础命令实验

### 2026-03-13

- 实验主题：第 1 周验收收口
- 现象：已结合宿主机上的两个基于 `alpine` 的 container object，完成对 image、container、process、container writable layer 关系的口头验收
- 关键入口：`docker ps -a`、`docker exec`、`docker rm`、宿主机中的 container 状态观察
- 遇到的问题：回答中一度过度依赖 `runtime object` 抽象表述，对 image 的理解仍偏向“目录”而不是“只读层 + 配置元数据”
- 当前结论：第 1 周可标记为完成；已明确 container 与 process 不是同一概念，多个 container 共享 lower layers、各自拥有 upper layer
- 下一次继续点：进入第 2 周，用最小 Dockerfile 和 `docker history` 观察镜像层、构建缓存与配置指令差异

### 2026-03-13

- 实验主题：第 1 周 inspect 视角补充
- 现象：围绕 `exited` container 的 `State`、`Config`、`HostConfig`、`NetworkSettings`、`LogPath` 等字段补充了运行时对象观察
- 关键入口：`docker inspect`、`State.Running`、`State.Status`、`State.Pid`、`ExitCode`、`FinishedAt`
- 遇到的问题：对 image 与 container 存储关联的描述仍有“目录化”倾向，对存储层保留信息的精度还不够
- 当前结论：已能够说明主进程结束不会自动删除 container object；`docker rm` 删除的是 container 对象及其关联状态，而不只是 PID 记录
- 下一次继续点：正式进入第 2 周，观察 Dockerfile 指令如何影响镜像层和构建缓存

### 2026-03-16

- 实验主题：第 2 周最小 Dockerfile 与 OverlayFS 第一轮观察
- 现象：通过最小 Dockerfile 验证了缓存链的顺序依赖；修改 `COPY` 输入会让后续步骤失去缓存，只修改末尾 `ENV` 只会让自身失去缓存；`docker history` 中 `ENV` 为 `0B`，而 `COPY`、`RUN` 对应非零大小
- 关键入口：`docker build`、`docker history`、`docker run`、`docker inspect`、`GraphDriver.Data.LowerDir`、`GraphDriver.Data.UpperDir`、`GraphDriver.Data.MergedDir`、`GraphDriver.Data.WorkDir`
- 遇到的问题：一开始容易把 image layer、container writable layer 和 `docker commit` 的输入语义混在一起，需要通过实际现象反复拆开
- 当前结论：image 由多层只读 lower layers 和配置元数据组成；container 拥有独立 upper layer；`merged` 视图优先看到 upperdir 中的同路径版本，因此运行时修改会遮住 lowerdir 中的原始文件，删除也不会真正改写 lowerdir
- 下一次继续点：继续追存储驱动细节，观察 lowerdir 文件在运行时被修改或删除时，upperdir 中会出现什么对应痕迹

### 2026-03-16

- 实验主题：第 2 周 OverlayFS 存储痕迹与最终验收
- 现象：在宿主机直接观察 `UpperDir` 时，修改 lower layer 文件会在 `UpperDir` 中出现对应路径的新版本文件；删除 lower layer 文件时，会出现同名的特殊字符设备占位对象 `c 0,0` 用于遮蔽 lowerdir 中的原始路径
- 关键入口：`docker inspect`、`GraphDriver.Data.UpperDir`、宿主机目录查看、`docker run` 中对 `/test.txt` 的修改与删除实验
- 遇到的问题：容易把“删除 lowerdir 文件”误解为真的改写只读层，或者把 image 简化成单个目录；需要通过宿主机上的实际存储痕迹纠正这个心智模型
- 当前结论：第 2 周可标记为完成；已能完整解释 `Dockerfile -> build cache -> image layers -> container upper layer -> OverlayFS merged` 这条链路，并确认 image 的可复用性与 container 的隔离性是如何同时成立的
- 下一次继续点：切换到第 3 周，开始 bridge 网络、容器互通与端口映射的最小实验

### 2026-03-16

- 实验主题：第 3 周 bridge 网络与端口映射第一轮观察
- 现象：默认 bridge 下按容器名 `ping` 会因名字解析失败而报 `bad address`，但按 IP 可以互通；自定义 bridge 下可以直接按容器名互相 `ping`；通过 `-p 宿主机端口:容器端口` 暴露 HTTP 服务后，宿主机可以访问到容器内响应
- 关键入口：默认 bridge、用户自定义 bridge、`ping`、容器名解析、容器 IP、`-p 8080:80`
- 遇到的问题：一开始容易把 bridge、veth pair、物理网卡角色混在一起，也容易把“名字解析失败”和“网络链路不通”混为一谈
- 当前结论：bridge 更像交换机，veth pair 更像连接 container 与宿主机 bridge 的虚拟网线；默认 bridge 具备基本 IP 连通性但不提供同样的容器名解析体验；端口映射的方向是宿主机端口转发到容器端口
- 下一次继续点：继续观察容器到宿主机再到外部访问路径，并把 bridge、容器 IP、宿主机监听端口之间的关系串起来

### 2026-03-17

- 实验主题：第 3 周宿主机访问路径与最终验收
- 现象：宿主机访问映射端口时，宿主机侧能看到 `docker-proxy` 在 `0.0.0.0` 上监听；不做 `-p` 时宿主机侧不会出现对应端口监听，但容器服务仍可通过 container IP 直接访问
- 关键入口：`docker-proxy`、宿主机监听端口、`0.0.0.0`、container IP、`-p 宿主机端口:容器端口`
- 遇到的问题：容易把“容器服务存在”与“宿主机存在显式访问入口”混为一谈，也容易误以为不做 `-p` 就代表容器服务没有正常启动
- 当前结论：第 3 周可标记为完成；已能区分默认 bridge 与自定义 bridge 的体验差异，理解宿主机端口映射是额外创建的访问入口，并明确 container 服务是否存在与是否做 `-p` 是两件不同的事
- 下一次继续点：切换到第 4 周，开始 bind mount、volume 与容器生命周期解耦的最小实验

### 2026-03-17

- 实验主题：第 4 周 bind mount、volume 与挂载路径验收
- 现象：`bind mount` 下，container 与宿主机对同一目录的修改可以双向立即可见，删除旧 container 后新 container 仍能看到同一份宿主机数据；`volume` 下，只有写入挂载点路径内的数据才会被新 container 继续看到，写到其他路径会误判为“volume 没生效”
- 关键入口：`bind mount`、`volume`、挂载点路径、container 可写层、宿主机目录
- 遇到的问题：一开始容易把“挂了 volume”误解成“container 内所有写入都会持久化”，实际只有落在挂载点里的数据才会进入 volume
- 当前结论：第 4 周可标记为完成；已能区分 bind mount 与 volume 在宿主机路径控制上的差异，理解二者都通过把数据放到 container 可写层之外来实现跨 container 生命周期保留
- 下一次继续点：切换到第 5 周，开始 namespace 的最小实验，并逐步用 Go 代码触达 `clone` / `unshare` 对应的能力入口

### 2026-03-18

- 实验主题：第 5 周 UTS 与 PID/Mount namespace 最小代码骨架
- 现象：`docker/uts/main.go` 已通过 `exec.Command("/bin/sh") + SysProcAttr.Cloneflags=CLONE_NEWUTS` 跑通 UTS 隔离；`docker/pid/main.go` 通过 `run/init` 两阶段结构进入 `CLONE_NEWPID | CLONE_NEWNS | CLONE_NEWUTS` 的新环境，并观察到 `echo $$` 与 `ps` 视图不一致，确认 `/proc` 仍是关键问题
- 关键入口：`docker/uts/main.go`、`docker/pid/main.go`、`exec.Cmd.SysProcAttr`、`syscall.CLONE_NEWUTS`、`syscall.CLONE_NEWPID`、`syscall.CLONE_NEWNS`、`/proc`、`syscall.Mount`
- 遇到的问题：一开始容易把 `PID namespace` 只理解为“PID 变成 1”，忽略了 `ps` 依赖 `/proc` 虚拟文件系统；也容易把 AI 提供的 `run/init` 结构当成黑盒，而不是去理解为什么必须先在新环境里做初始化再 `exec` 成真正的 `/bin/sh`
- 当前结论：第 5 周已正式开始且进入中段；已明确 `PID namespace` 会把实验从单纯的 `Cloneflags` 推进到 `/proc` 挂载处理，`run/init` 两阶段骨架值得作为后续最小容器雏形的基础实验资产保留
- 下一次继续点：继续完善 `PID + Mount namespace` 下的 `/proc` 处理动作链，并单独拆出 `docker/mount/main.go` 去观察挂载视图本身

### 2026-03-18

- 实验主题：第 5 周独立 Mount namespace 骨架与挂载视图观察
- 现象：通过手工 `unshare -m` 实验观察到，在新 mount namespace 中对实验目录挂载 `tmpfs` 后，新 shell 内能看到新的挂载和目录内容，而宿主机同一路径既看不到这次挂载，也看不到在该路径下新建的文件；同时已写出 `docker/mount/main.go` 的独立 `run/init + CLONE_NEWNS + init 阶段挂载 + exec sh` 最小骨架
- 关键入口：`unshare -m`、`mount -t tmpfs ...`、`mount | grep <path>`、`docker/mount/main.go`、`syscall.CLONE_NEWNS`、`syscall.Mount`
- 遇到的问题：一开始容易把 mount namespace 的隔离误解成“目录内容不同”，而不是“同一路径背后的挂载关系不同”；也容易把挂载点准备这种外围动作和 mount namespace 主线混在一起
- 当前结论：已能独立说明 mount namespace 隔离的是路径到文件系统的挂载关系，而不只是普通文件内容；`docker/mount/main.go` 已具备作为第 5 周第三条最小机制实验骨架保留的条件
- 下一次继续点：把 UTS、PID、Mount 三条实验线汇总收口，明确它们如何自然拼成一个“最小容器初始化链”

### 2026-03-18

- 实验主题：第 5 周最小容器初始化链收口
- 现象：已能用 `docker/uts/main.go`、`docker/pid/main.go`、`docker/mount/main.go` 三条最小实验骨架反推出统一结构：`parent` 负责创建带 namespace 的新进程，`init` 负责在新环境中完成 `/proc`、挂载点等初始化动作，再 `exec` 成真正的目标进程
- 关键入口：`docker/uts/main.go`、`docker/pid/main.go`、`docker/mount/main.go`、`run/init` 两阶段、`exec /bin/sh`
- 遇到的问题：最初容易把三条实验线看成彼此独立的 demo，而不是自然收敛为“最小容器初始化链”；也容易把 AI 提供的 `run/init` 结构当成答案，而不是去理解为什么这种结构是被约束逼出来的
- 当前结论：第 5 周可标记为完成；已能解释为什么最小容器雏形不会只是“直接起一个带 flag 的 shell”，而会自然长成 `parent -> init -> exec target` 的初始化结构
- 下一次继续点：切换到第 6 周，开始 cgroup、capabilities 与“namespace 不等于资源限制”的最小实验

### 2026-03-18

- 实验主题：第 6 周 cgroup v2、capabilities 与容器安全边界
- 现象：本机使用 `cgroup v2`；为容器设置 `--memory=128m` 后，`HostConfig.Memory` 与 cgroup 路径下的 `memory.max` 对应为同一字节值；普通容器内执行 `mount` 和修改 `hostname` 都因权限不足失败
- 关键入口：`/proc/<PID>/cgroup`、`/sys/fs/cgroup/.../memory.max`、`docker inspect -f '{{.HostConfig.Memory}}'`、container 内的 `mount`、`hostname`
- 遇到的问题：容易把“进了 namespace”误解成“已经被限制资源”或“自动拥有 root 的全部能力”；也容易把 cgroup 的路径归属和资源限制本身混为一谈
- 当前结论：第 6 周可标记为完成；已能区分 namespace、cgroup、capabilities 三者的职责边界，明确 namespace 负责隔离视图，cgroup 负责资源限制，capabilities 负责特权动作裁剪，因此容器不是天然完整安全边界
- 下一次继续点：切换到第 7 周，先准备本地 Kubernetes 环境，再开始 Pod、Deployment、Service 的最小实验

### 2026-03-19

- 实验主题：第 7 周 Pod、Deployment、Service 第一轮验收
- 现象：本地 kind 集群创建成功后，单独创建的 Pod 能进入 Running、被调度到 node 并获得 Pod IP，但删除后不会自动重建；Deployment 会自动创建 Pod、维持副本数并在 Pod 被删后补回；Service 为一组 Pod 提供独立的 `CLUSTER-IP` 和稳定入口，endpoints 会随 Pod 变化更新
- 关键入口：`kind create cluster`、`kubectl run`、`kubectl create deployment`、`kubectl scale deployment`、`kubectl expose deployment`、`kubectl get pods -o wide`、`kubectl get svc`、`kubectl get endpoints`
- 遇到的问题：一开始容易把 Pod 当成“container 的别名”，或者把 node 和 pod 的关系理解成静态包含关系；也容易把 Pod、Deployment、Service 三者当成并列但互不关联的对象
- 当前结论：第 7 周可标记为完成；已能说明 Pod 是 K8s 的最小调度单元，Deployment 负责维持期望状态，Service 负责给一组 Pod 提供稳定访问入口
- 下一次继续点：切换到第 8 周，开始 ConfigMap、Secret、Volume 与声明式配置实验

### 2026-03-19

- 实验主题：第 8 周 ConfigMap、Secret、Volume 与声明式配置
- 现象：`ConfigMap` 可作为环境变量和文件挂载两种方式注入 Pod；更新同一个 `ConfigMap` 后，环境变量注入的 Pod 仍保持旧值，而以 volume 挂载读取文件的 Pod 可以看到新值；`Secret` 也可注入 Pod，但在对象展示层面不会像 `ConfigMap` 那样直接明文暴露值
- 关键入口：`kubectl create configmap`、`kubectl create secret generic`、`env.valueFrom.configMapKeyRef`、`env.valueFrom.secretKeyRef`、`volumes.configMap`、`volumeMounts`
- 遇到的问题：一开始容易把 `ConfigMap` 当成某种配置文件格式本身，或者把 `ConfigMap/Secret + volume` 误解成“持久化业务数据”；实际这里的重点是运行时配置注入，而不是业务数据持久化
- 当前结论：第 8 周可标记为完成；已能区分配置注入与镜像固化的边界，理解 `ConfigMap/Secret` 可通过环境变量或挂载文件在运行时映射进 Pod，且不同注入方式具有不同更新行为
- 下一次继续点：切换到第 9 周，开始 requests、limits 与 Pod 调度 / OOM / probe 的最小实验

### 2026-03-19

- 实验主题：第 9 周 requests、limits、Pending 与 OOMKilled 第一轮观察
- 现象：在 node 资源视图中可以看到明确的 CPU / memory `Capacity` 与 `Allocatable`；带明显过大 `requests.memory` 的 Pod 会停在 `Pending` 并报 `Insufficient memory`；同时设置 `requests` 与 `limits` 时，两者会在 Pod 资源配置中分别记录；超出 `memory limit` 的 Pod 最终进入 `OOMKilled`
- 关键入口：`kubectl describe node`、`resources.requests`、`resources.limits`、`kubectl describe pod`、`Pending`、`OOMKilled`
- 遇到的问题：一开始容易把 `StartError` 误判为 OOM，实际需要通过容器状态和错误原因区分“命令启动失败”和“运行中超出 limit 被杀”；也容易把 `requests` 和 `limits` 混成同一种资源配置
- 当前结论：已明确 `requests` 主要影响调度阶段，资源请求过大时 Pod 会因节点资源不足而无法调度；`limits` 主要影响运行阶段，超限后容器会被内核 / runtime 以 OOMKilled 等方式终止
- 下一次继续点：继续第 9 周 probe 实验，观察 liveness/readiness 失败为何会触发容器重建或流量摘除

### 2026-03-24

- 实验主题：第 9 周 readiness / liveness probe、Deployment 滚动替换与事件链观察
- 现象：为 `nginx` 配置错误的 `readinessProbe.httpGet.path` 后，Pod 处于 `Running` 但 `READY 0/1`，`describe pod` 中出现 `Readiness probe failed: HTTP probe failed with statuscode: 404`，且 `Service endpoints` 不包含该 Pod；将同一实验切换到 `Deployment` 后，修改 `spec.template` 中的 probe 配置会触发新的 `ReplicaSet` 和新 Pod 创建，旧 Pod 被替换；在 `readiness` 正常的前提下再加入故意失败的 `livenessProbe` 后，事件中连续出现 `Liveness probe failed`、`Killing`、`Readiness probe failed: connect: connection refused` 与 `BackOff`
- 关键入口：`readinessProbe.httpGet`、`livenessProbe.httpGet`、`kubectl describe pod`、`kubectl get endpoints`、`kubectl get rs`、`kubectl rollout status deployment/<name>`
- 遇到的问题：最初对 probe 的职责边界没有拆开，容易把“摘流量”和“重启容器”混成同一类健康检查；也一度把 Pod/ReplicaSet 理解成可原地热改的对象，忽略了 `Deployment.spec.template` 变化会导向新的 `ReplicaSet`
- 当前结论：`readiness` 失败主要改变的是 Pod 是否作为 ready backend 接入 `Service` 流量，容器进程可以继续存活；`liveness` 失败会直接触发 kubelet 重启容器；当容器已被 `liveness` 打入重启流程时，`readiness` 也会因端口未监听而跟随失败，但这不是重启主因；同时已确认 `Deployment` 更新的是 Pod 模板版本，因此通过新 `ReplicaSet` 创建新 Pod 替换旧 Pod，而不是在原 Pod 上热改 probe 配置
- 下一次继续点：基于 `Pending`、`OOMKilled`、`readiness failed`、`liveness failed` 四类现象做第 9 周最终验收，收紧“调度 / 资源限制 / 流量接入 / 生命周期重启”四层边界

### 2026-03-24

- 实验主题：第 10 周 Pod IP 直连、Service ClusterIP 与 Endpoints 第一轮观察
- 现象：`pod-a` 可以直接通过 `pod-b` 的 Pod IP 访问到 `nginx` 页面，整个过程中没有为 `pod-b` 配置 `Service` 或宿主机端口映射；为 `pod-b` 打 label 并创建最小 `Service` 后，`pod-a` 也可以通过 `Service ClusterIP` 访问到同一服务，且 `Service endpoints` 指向 `pod-b` 的 Pod IP；删除裸 `pod-b` 后，`Service` 对象仍存在，但 `endpoints` 变为 `<none>`；改为 `Deployment + Service` 组合后，删除后端 Pod 时，`Deployment` 会补出新 Pod，`endpoints` 随之重新指向新 Pod IP
- 关键入口：`kubectl get pods -o wide`、`kubectl exec <pod> -- wget -qO- http://<PodIP>`、`kubectl get svc`、`kubectl get endpoints`、`kubectl delete pod`、`kubectl get pods -w`
- 遇到的问题：最初不会写最小 `Service`/`Deployment` 结构，容易把 `Service` 理解成“让 Pod 首次可通信”的前提，也容易把 `ClusterIP`、`Pod IP` 与 `endpoints` 混成一个地址概念
- 当前结论：Pod 间直接通信依赖的是集群内 Pod IP 可达性，而不是宿主机端口映射；`Service` 提供的是稳定入口，真正的后端地址记录在 `endpoints` 中；`Deployment` 负责补后端实例，`Service` 负责追踪并转发到当前匹配 selector 的 Pod 集合，因此 `ClusterIP` 可以稳定不变，而 `endpoints` 中的 Pod IP 会随 Pod 重建而变化
- 下一次继续点：观察 Pod 网段、node 网段与 Service ClusterIP 地址段的分布，开始拆开 Pod 网络、Service 虚拟地址与节点网络之间的关系

### 2026-03-25

- 实验主题：第 10 周 Pod 网络地址池、Service 虚拟地址与 `iptables` 转发链进一步观察
- 现象：当前单节点 kind 集群中，node `INTERNAL-IP` 位于 `172.21.0.x` 网段，Pod IP 位于 `10.244.0.x` 网段，`Service ClusterIP` 位于 `10.96.x.x` 网段；控制面参数中可直接观察到 `kube-controller-manager` 使用 `--cluster-cidr=10.244.0.0/16`，`kube-apiserver` 使用 `--service-cluster-ip-range=10.96.0.0/16`；`kubectl describe node` 中可见该节点的 `PodCIDR: 10.244.0.0/24`；`kube-proxy` 日志显示当前使用 `iptables Proxier`；在节点 `nat` 表中，访问 `Service ClusterIP` 会命中对应的 `KUBE-SVC...` 链，再跳到 `KUBE-SEP...` 链，并最终 `DNAT` 到真实的 `Pod IP:80`；而节点路由表中可以直接看到若干 `10.244.0.x dev veth... scope host` 形式的本地 Pod 主机路由
- 关键入口：`kubectl get nodes -o wide`、`kubectl get pods -o wide`、`kubectl get svc`、`kubectl describe node`、`kubectl logs -n kube-system <kube-proxy-pod>`、`docker exec <kind-node> iptables -t nat -L -n`、`docker exec <kind-node> ip route`、`kube-apiserver`/`kube-controller-manager` 启动参数中的 `service-cluster-ip-range` 与 `cluster-cidr`
- 遇到的问题：最初容易把出现在 `iptables` 输出中的 Pod IP 误解为“直接访问 Pod IP 也要走 Service 规则链”；也一度把 `ClusterIP` 当成某个真实接口地址，而不是由 `kube-proxy + iptables` 提供的虚拟入口；在观察 `kindnet` 时，若命令没有保留列名，用户需要额外猜测字段位置
- 当前结论：第 10 周目前已能把两条链严格拆开：`Pod 网络` 负责 `cluster-cidr -> PodCIDR -> Pod IP -> veth/route` 这条真实可达路径，`Service` 负责 `service-cluster-ip-range -> ClusterIP -> kube-proxy -> iptables DNAT -> Pod IP` 这条虚拟转发路径；在单节点环境中，本地 Pod 互通可以直接落在主机路由和 `veth` 上，而不需要经过 `Service` 的 `KUBE-SVC/KUBE-SEP` 转发表
- 下一次继续点：进入最小多节点 kind 环境，观察远端 node 上的 Pod 不再是本地 `veth` 直达目标时，节点为何必须额外知道其他 Pod 网段该如何转发

### 2026-03-25

- 实验主题：第 10 周新建单节点 kind 集群后的 `kindnet` / CNI 配置观察
- 现象：重新创建单节点 `infra-learning` kind 集群后，node / Pod / Service 三类地址仍分别落在 `172.21.0.x`、`10.244.0.x`、`10.96.0.x` 三套网段；通过 `kubectl run` 与 `kubectl expose pod` 复现了 `Service ClusterIP -> endpoints -> Pod IP` 的最小访问链；`kube-proxy` 日志再次确认当前为 `Using iptables Proxier`；`kindnet` 的 `DaemonSet` 使用 `hostNetwork: true`，并把节点上的 `/etc/cni/net.d` 挂载进容器；节点 `/etc/cni/net.d/10-kindnet.conflist` 中可见主插件为 `"type": "ptp"`，`ipam.type` 为 `"host-local"`，并且地址池直接来自当前节点的 `subnet: "10.244.0.0/24"`，同时还包含 `"type": "portmap"` 的附加插件
- 关键入口：`kubectl run`、`kubectl expose pod`、`kubectl get pods -o wide`、`kubectl get svc`、`kubectl get endpoints`、`kubectl logs -n kube-system <kube-proxy-pod>`、`kubectl get daemonset -n kube-system kindnet -o yaml`、`docker exec <kind-node> cat /etc/cni/net.d/10-kindnet.conflist`
- 遇到的问题：最初误以为 `kindnet` 的 CNI 配置会单独存放在某个 `ConfigMap` 中；在给出观察命令时若没有保留列名或明确输出字段，用户需要自行猜测列位置，容易造成无意义阻塞；一次性抛出过多字段会让“Pod 网络”和“Service 转发”两条线再次混在一起
- 当前结论：已能进一步收紧第 10 周的职责边界：`CNI/kindnet` 这条线负责把 Pod 网络配置安装到节点上，并按 `PodCIDR` 通过 `host-local IPAM` 给 Pod 分配真实可达的 Pod IP；`kube-proxy` 这条线负责把 `Service ClusterIP` 这种虚拟入口流量通过 `iptables` 转到当前后端 Pod。两条机制都依赖节点网络视角，但解决的问题不同
- 下一次继续点：继续在单节点里观察 `ptp + veth` 这对点到点连接是如何对应到具体 Pod 网络接口上的，把 `CNI 配置 -> veth -> Pod 内 eth0` 这条链补齐

## 4. 概念盲区清单

当前待持续补齐的概念：

- namespace 的分类与边界
- cgroup v1 与 cgroup v2 的差异
- Docker CLI 与 Docker daemon 的边界
- pivot_root 与 chroot 的区别
- OverlayFS 与镜像分层的映射关系
- veth pair、bridge、iptables 之间的关系
- containerd、runc、OCI 的职责边界
- CRI、CNI、CSI 分别解决什么问题
- pause container 的作用

## 5. 每周复盘模板

每完成一周，至少补一段：

### 第 N 周复盘

- 我现在能解释什么：
- 我还解释不了什么：
- 我做过的关键实验：
- 我接下来最需要补的知识点：

## 6. 衔接规则

以后每次切换终端或继续学习时，优先看这里：

1. `当前阶段`
2. `下一步`
3. `最近一次实验记录`
4. `概念盲区清单`

如果某次学习中途被打断，至少补一条实验记录，避免上下文断裂。

# Docker 与 Kubernetes 学习路线

## 1. 文档说明

这份路线用于系统学习 Docker 与 Kubernetes 的应用和底层原理，主线顺序固定为：

1. 先学 Docker
2. 再学 Kubernetes
3. 中间穿插容器相关的操作系统知识

学习方法采用双线并行：

- 应用线：先做实验，观察现象，能使用、能验证、能排错
- 底层线：追到 Linux 内核机制、容器运行时链路和关键源码入口

配套使用方式：

- 这份文档负责记录稳定路线，不记录零散状态
- 实际学习状态、实验结论、阻塞点统一写入 `学习进度.md`

## 2. 总体阶段图

### Phase 0：环境与前置认知

目标：

- 明确容器不是虚拟机
- 建立 Linux 进程、文件系统、网络命名空间的基本心智模型
- 准备 Docker 与 Kubernetes 的实验环境

### Phase 1：Docker 基础使用

目标：

- 熟悉镜像、容器、Dockerfile、网络、卷等基础能力
- 建立“一个容器到底是什么”的最小认知

### Phase 2：Docker 底层机制

目标：

- 追到 namespace、cgroup、rootfs、镜像分层
- 理解 Docker、containerd、runc 的职责划分

### Phase 3：镜像、网络、存储与安全

目标：

- 将前面的零散知识串成完整容器运行链路
- 理解网络、挂载、安全隔离是如何落到内核上的

### Phase 4：Kubernetes 应用基础

目标：

- 掌握 Pod、Deployment、Service、ConfigMap、Volume 等核心对象
- 用声明式方式组织容器工作负载

### Phase 5：Kubernetes 控制面与运行时链路

目标：

- 理解 kubelet、apiserver、scheduler、controller-manager、etcd 的协作
- 理解 K8s 如何最终调到 containerd / runc / CNI / CSI

## 3. 12 周详细路线

### 第 1 周：Docker 基础命令与容器生命周期

应用目标：

- 熟悉 `docker pull`、`docker run`、`docker ps`、`docker exec`、`docker logs`、`docker rm`
- 理解镜像与容器的关系

底层目标：

- 建立 Linux 进程与宿主机资源视角
- 思考“容器进程”和普通进程的差异到底在哪里

操作系统知识：

- 进程、父子进程、PID、前台与后台
- 根文件系统与进程视角

输出要求：

- 用自己的话解释镜像、容器、进程三者关系

### 第 2 周：Dockerfile、镜像分层与 OverlayFS 初识

应用目标：

- 编写基础 Dockerfile
- 理解 build、layer、cache

底层目标：

- 理解镜像分层不是“打包目录”这么简单
- 初识 Union FS / OverlayFS

操作系统知识：

- 文件系统层次
- mount 基础概念

输出要求：

- 解释为什么修改一层不会重写整个镜像

### 第 3 周：容器网络与端口映射

应用目标：

- 使用 bridge 网络
- 理解端口映射、容器互通

底层目标：

- 理解 veth pair、bridge、network namespace
- 观察容器网络如何回到宿主机网络栈

操作系统知识：

- 网络命名空间
- 网卡、路由、NAT 的基本概念

输出要求：

- 能画出容器到宿主机再到外部网络的最小路径

### 第 4 周：Volume、Bind Mount 与挂载视角

应用目标：

- 使用 volume 和 bind mount
- 理解数据持久化与容器生命周期解耦

底层目标：

- 理解 mount namespace、rootfs、挂载点
- 认识挂载传播与容器文件系统边界

操作系统知识：

- mount、umount
- 目录树与挂载点

输出要求：

- 解释 bind mount 和 image layer 的本质区别

### 第 5 周：Namespace 深入与最小容器雏形

应用目标：

- 用 Go 手工触达 UTS、PID、Mount、IPC、Network namespace
- 观察隔离前后行为差异

底层目标：

- 理解 `clone` / `unshare` / `setns` 的角色
- 明确不同 namespace 隔离的对象是什么

操作系统知识：

- namespace 分类
- `/proc` 与进程视图

输出要求：

- 能解释为什么 PID namespace 经常要配合新的 `/proc`

### 第 6 周：cgroup、capabilities 与容器安全边界

应用目标：

- 观察 CPU / memory 限制
- 理解容器并不是天然安全边界

底层目标：

- 理解 cgroup v1/v2 基础作用
- 理解 capabilities、seccomp、只读文件系统等机制

操作系统知识：

- 资源限制
- 权限模型

输出要求：

- 解释 namespace 为什么不等于 resource limit

### 第 7 周：Kubernetes 核心对象入门

应用目标：

- 运行 Pod、Deployment、Service
- 理解期望状态与实际状态

底层目标：

- 建立控制循环（control loop）的基本认知
- 理解 K8s 不是“高级 Docker 命令集合”

操作系统知识：

- 进程监督
- 服务发现的系统视角

输出要求：

- 用一句话解释 Pod 为什么不是单纯的容器别名

### 第 8 周：ConfigMap、Secret、Volume 与声明式配置

应用目标：

- 管理配置、密钥、持久化数据
- 理解 YAML 资源声明与变更

底层目标：

- 理解 K8s 如何把配置与存储映射给容器
- 初识 CSI 与 Volume 挂载链路

操作系统知识：

- 文件、环境变量、挂载之间的关系

输出要求：

- 解释“配置注入”和“镜像固化”的边界

### 第 9 周：调度、资源请求限制与探针

应用目标：

- 使用 requests / limits
- 使用 liveness / readiness probe

底层目标：

- 理解调度依据、节点资源视角
- 理解 probe 失败为何会触发容器重建

操作系统知识：

- CPU、内存、OOM、进程退出码

输出要求：

- 解释 `requests` 和 `limits` 与 cgroup 的关系

### 第 10 周：Kubernetes 网络模型

应用目标：

- 理解 Service、Ingress、ClusterIP、NodePort
- 建立集群内通信视角

底层目标：

- 初识 CNI
- 理解 K8s 网络为何要求 Pod 间可直接互通

操作系统知识：

- 路由、iptables/ipvs、虚拟网络设备

输出要求：

- 解释 Service 为什么不是“真实进程”

### 第 11 周：Kubelet、CRI、containerd、runc 调用链

应用目标：

- 从一个 Pod 创建事件追踪到容器运行

底层目标：

- 理解 kubelet 如何调用 CRI
- 理解 containerd 与 runc 的边界

操作系统知识：

- 进程创建链路
- Linux runtime 与 OCI 的关系

输出要求：

- 画出从 PodSpec 到容器进程的最小链路

### 第 12 周：控制面与综合复盘

应用目标：

- 串起 apiserver、scheduler、controller-manager、etcd 的职责
- 用完整链路复盘容器编排系统

底层目标：

- 理解声明式系统如何通过事件与循环达成收敛

操作系统知识：

- 分布式状态与本地进程执行的边界

输出要求：

- 用自己的话解释 K8s 为什么需要控制面，而 Docker 不需要

## 4. 每周固定学习模板

每周都按下面的顺序推进：

1. 先做一个最小应用实验
2. 再追底层机制
3. 最后写一段复盘

每周至少回答这五个问题：

- 这周的主题是什么
- 我做了哪些实验
- 我观察到了什么现象
- 这些现象对应的底层机制是什么
- 我还解释不了什么

## 5. 推荐实验主线

建议贯穿全程保留三条实验线：

### 实验线 A：从 Docker 使用到最小容器雏形

- 从 `docker run` 开始
- 逐步追到 Go + namespace + cgroup 的最小实验

### 实验线 B：从镜像到运行时链路

- 从 Dockerfile 和 image layer 开始
- 逐步追到 Docker CLI、containerd、runc、OCI

### 实验线 C：从单机容器到集群编排

- 从单容器应用开始
- 过渡到 Pod、Deployment、Service、调度与控制循环

## 6. 阶段验收标准

完成 Docker 阶段后，你至少应当能解释：

- 镜像、容器、进程之间的关系
- namespace 和 cgroup 分别解决什么问题
- volume、bind mount、image layer 之间的本质差异
- Docker、containerd、runc 的职责边界

完成 Kubernetes 阶段后，你至少应当能解释：

- Pod、Deployment、Service 分别解决什么问题
- K8s 为什么需要调度器和控制器
- kubelet、CRI、containerd、runc 如何串起来
- CNI / CSI / CRI 分别位于哪一层

完成整个路线后，你至少应当能回答：

- 容器本质上是不是一个特殊进程
- Docker 到底替你封装了哪些 Linux 复杂性
- Kubernetes 相比 Docker 单机管理，多出的核心能力到底是什么
